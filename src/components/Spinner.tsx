import React from "react";

interface Props {}

const Spinner = () => {
  return (
    <div className="mt-[10%] w-full">
      <img src="/assets/Spinner.gif" className="mx-auto" />
    </div>
  );
};

export default Spinner;
